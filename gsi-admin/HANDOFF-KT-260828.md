# KT 8/27·8/28 반영사항 — gsi-admin 반영 현황 · 개발사 인수 항목 (26-08-29)

출처: 26-08-27 KT 피드백 6건 및 26-08-28 KT 요구 10건 · 대조: 커밋 `b8f2546`·`a263ee3`·`16e80d1`·`21bcf70`·`01ebe9e`

## 반영 완료 — 화면 검증 기준, 데이터는 더미

| # | 요구 | 구현 위치 | 더미·전제 |
|---|---|---|---|
| 8/27-1 | 리스트 뷰에 일반채널 재핑 표시 | `app.js` `zapList()`·`renderList()` · `ui.css` `.plist__tv`·`.plist__zap`·`.plist__zap .zk` | 타임라인과 같은 결정적 후보 계산을 공유. 프로그램 종료 직전 1~3분, 임계값·값은 더미이며 실제 재핑 원천값으로 교체 필요 |
| 8/27-2 | 분석 대상 프로그램을 편성표 5개 + 상세 5개, 총 10개로 제한 | `app.js` `BASKET_MAX`·`toggleBasket()` · `analysis.html` 주입 레이어 `X_MAX`·`OWN_MAX`·`openOwn()` | 편성표와 상세의 5칸을 합산한 화면 상한. 상세 본체는 KT 번들이고 상한만 주입 레이어에서 관리 |
| 8/27-3 | 주요 지표 「전체 콜 수」 한 박스에 콜 수와 웹 반응수를 병기 | `analysis.html` `gsiIntegWebJs` `kpi()` · `.ktkpi2` | 웹 반응수는 통합 차트에서 계산한 `LAST.total` 합계. 웹 값은 결정적 더미이며 실데이터 집계 규칙 필요 |
| 8/27-4 | 통합 분석 차트에 옅은 붉은 재핑, 범례 두 묶음, 웹 반응수 누적 막대와 툴팁 행 추가 | `analysis.html` `gsiIntegWebJs` `bars()`·`tip()` · `.ktzap-seg`·`.ktweb`·`.ktweb-row` | 콜 아래·웹 위로 쌓음. 웹 값은 `webOf()`의 콜 대비 1.2~2.2배 결정적 더미. 재핑 칸은 KT 합계를 분할한 화면 값 |
| 8/27-5 | 동일 시간대 17개사 편성 성과 표를 7개에서 10개 컬럼으로 확대 | `analysis.html` `gsiTable17Js` · `data-ktc` 이름표 · `gsiCrossAnalysisJs` `rowTarget()` | 프로그램명·시청자수(10초UV평균)·웹 반응 수 컬럼을 추가. 웹 반응 수는 편성표 표의 더미 규칙을 사용 |
| 8/27-6 | 최근 1개월 히트맵 호버에 채널명·웹 반응수 추가, 자사/타사 포함 전환, 셀 고정 및 담기 | `analysis.html` `gsiCrossAnalysisJs` · `kth2-script` `kth2-sw-own`·`kth2-sw-all`·`kth2-pin` | 같은 화면에서 자사 성과/타사 포함을 전환. 호버 행을 누르면 프로그램 전환 바에 담김 |
| 8/28-2 | 분석 화면 차트 범례를 눌러 계열을 켜고 끄기 | `analysis.html` `gsiLegendToggleJs` `legends()`·`apply()` · `.ktlgx`·`.ktlgx-off`·`.ktlgx-na` | 선·막대 계열과 범례 칩을 색/순서로 대응. 유입·유출의 `SBS`는 대응 계열이 없어 비활성 칩 |
| 8/28-5a | 당일 조회 시 방송 중 프로그램을 리스트 첫 행에 고정하고 `ON AIR` 표시 | `app.js` `TODAY`·`renderList()` 정렬·`.plist__row.is-onair` · `ui.css` `.tl__onair` | 당일 기본 조회 범위가 `TODAY`까지 포함. 배지는 타임라인과 동일한 `.tl__onair` 사용 |
| 8/28-5b | 편성표 리스트 호버에 콜 수 옆 웹 반응수 추가 | `app.js` `showPop()` `.pop__stats--4` · `ui.css` `.pop__stats--4` | `e.web` 더미를 사용하며 콜과 함께 집계 중 처리 |
| 8/28-7a | 통합 분석 범례에서 콜수와 웹반응수를 각각 켜고 끄기 | `analysis.html` `gsiIntegWebJs` `legend()`·`bars()` · `.ktlg-web`·`.ktweb-src` | KT 원본 콜 칩과 주입 웹 칩을 분리. 각 계열을 끄면 해당 막대만 사라지고 축은 켜진 값에 맞춤 |
| 8/28-7b | 유입 채널이 최대 8개일 때 범례 위치·줄바꿈 유지 | `analysis.html` `gsiIntegWebJs` `legend()` · `.ktlg-br`·`.ktlg-br.is-on` | 채널 묶음 경계에서만 줄바꿈. 차트 높이를 눌러 범례를 수용하지 않음 |
| 8/28-7c | 차트와 재핑의 시간대 눈금을 하나만 노출 | `analysis.html` `gsiIntegWebJs` `times()` · `.ktzap-times` | 재핑 띠의 눈금을 감추고 차트 x축만 유지. 두 좌표는 실측 기준 정렬 |
| 8/28-8a | 최근 1개월 히트맵 호버창을 카드 안에서 배치 | `analysis.html` `kth2-script` `place()` | 위·아래 여유를 비교해 한 자리를 선택하고, 둘 다 부족하면 카드 안쪽으로 밀어 넣음. 가로 위치도 카드 경계 안으로 제한 |
| 8/28-8b | 호버창 목록은 5개까지 보이고 6개부터 스크롤 | `analysis.html` `#kth2-card [class*="group-hover:block"] .max-h-48` | 최대 높이 282px. 6번째 항목부터 내부 스크롤 |
| 8/28-8c | 호버창 줄에 담김 표시 노출 | `analysis.html` `.ktheat-pop-row > span:not(.ktx-go)` · `.ktx-go` | 값 span 숨김 규칙에서 담김 배지를 예외 처리. 배지 노드는 `gsiCrossAnalysisJs`가 삽입 |

## 추가 정리

| 항목 | 내용 |
|---|---|
| 범례 칩 통일(우리 쪽 정리, KT 요구 아님) | 커밋 `21bcf70` — 범례 CSS `.ktlgx`·`.ktlg-b`·`.ktlg-cap`. 8/28 on/off 동작을 붙이면서 분석 화면의 범례 여섯 곳을 `.ktlgx` 계열 칩 모양으로 통일하고, 꺼진 계열은 `.ktlgx-off`로 흐리게 표시 |
| 콜·웹 표시 | 8/27 한 칩 안의 두 색 표시를 8/28 두 칩으로 분리. `gsiIntegWebJs`는 원본 막대를 삭제하지 않고 `.ktweb-src`로 감춰 재렌더를 보존 |
| 호버창 자리 규칙 | `kth2-script`의 `place()`가 카드 위·아래·내부를 상황별로 선택. 기존처럼 특정 방향 하나를 고정하지 않음 |
| 담김 배지 | `.ktheat-pop-row > span`처럼 넓은 선택자가 `.ktx-go`까지 숨기던 문제를 `:not(.ktx-go)`로 수정 |
| 호버창 높이 | 300px에서 6번째 줄이 잘려 보이던 문제를 282px로 조정해 5줄 표시 기준으로 정리 |
| 리스트 on air 표식 | 리스트 왼쪽의 중복 붉은 표식을 제거하고 `.tl__onair` 배지만 유지 |

## KT 확인 필요

| # | 확인 항목 | 현재 화면 전제 |
|---|---|---|
| 1 | 상세 화면의 담기 5칸을 17개사 표와 히트맵 호버창이 함께 사용할지, 히트맵 몫을 별도로 둘지 | 현재는 편성표 5개와 상세 5개를 별도 상한으로 두되, 상세 안의 표·히트맵은 같은 담기 상태를 공유 |
| 2 | A-8 「날짜·시간순 정렬」의 대상이 호버창의 편성 줄인지 히트맵 칸 배열인지 | 호버창 줄은 현재 날짜순. 칸 배열 정렬 요구라면 기준 캡처 또는 배열 규칙 확인 필요 |
| 3 | 재핑이 1주일 전 데이터로 대체될 때 화면에 그 사실을 표시할지 | 현재 화면에는 대체 집계 여부를 알리는 별도 표식 없음 |

## 개발사 인수 항목

| # | 개발사 구현·검토 항목 | 화면 쪽 전제 및 인수 시 주의 |
|---|---|---|
| 1 | 모든 시청률을 10초UV평균으로 통일하고, 프로그램 UV는 상세에서만 사용하며, 카테고리는 대분류 공통카테고리 기준으로 집계 | 17개사 표의 컬럼명 `시청자수(10초UV평균)`은 현재 규칙과 맞음. 화면의 결정적 더미를 실데이터 필드로 교체 |
| 3 | 재핑은 일반 프로그램만 노출, 당일은 30분 주기 집계, 당일 데이터가 없으면 1주일 전 데이터 사용 | 타임라인은 일반채널 레인에만 `.tl__zap`을 표시. `analysis.html`의 `.ktzap-seg` 값은 원본 합계 분할 더미이므로 1분 단위 원천값을 연결 |
| 4 | 당일 프로그램 전체를 최대 340행(17채널 × 20편)까지 조회 결과에 노출 가능한지 검토 | 현재 `renderList()`는 화면 리스트와 페이지 단위 렌더를 사용. 340행의 스크롤 길이와 페이지네이션·가상 스크롤 여부를 결정 |
| 6 | 주요 지표 「분최고 유입자수 / 시간」 쿼리 속도 확인 | 결과가 늦어질 때도 화면의 주요 지표·차트 로딩 상태와 순서를 유지할 수 있어야 함 |
| 11 | 타사 홈쇼핑 로고가 없을 때 외부 favicon URL 로 대체하는 코드(`analysis.html` `gsiTable17Js` 부근) | 운영 환경에선 외부 요청이 된다 — 자사 자산(`assets/brand/`)으로 교체하거나 텍스트 배지로 대체 |
| 10 | 엑셀 다운로드 양식과 라이브러리 확인 | 편성표의 `#btnExcel`은 버튼·토스트 화면만 있으며 xlsx 생성은 서버 몫. 기존 양식과 파일명 규칙을 연결 |

## 미반영(남은 UI)

| # | 항목 | 현재 상태 |
|---|---|---|
| A-8 | 최근 1개월 히트맵의 날짜·시간순 정렬 | 호버창 줄은 날짜순이나, 요구 대상이 칸 배열인지 확인 전이라 미반영 |
| A-8 | 프로그램을 5개 이상 담을 때 박스 크기를 유지하고 스크롤 버튼으로 추가 담기 | 현재 프로그램 전환 바가 5개 초과 시 한 줄 고정·좌우 스크롤 버튼을 제공하지 않음 |
| A-9 | IA 메뉴명 `시청행태 분석 > (구) 프로그램별` 및 `시청행태 분석 > 프로그램별` 두 갈래 | 기존 페이지를 삭제하지 않고 메뉴명만 두 갈래로 나누는 작업이 남음. `(구)`는 3개월 유지 후 삭제 예정 |
